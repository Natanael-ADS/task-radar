import '../app_di.dart';

Future<void> registerExternalModule(AppDi sl) async {
  // Colocar aqui integrações externas (ex.: analytics, crash reporting,
  // serviços do device) quando fizer sentido existir no app.
}
